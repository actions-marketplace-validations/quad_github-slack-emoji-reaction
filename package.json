{
	"name": "github-slack-emoji-reaction",
	"version": "2.0.0",
	"description": "GitHub Action that reacts on Slack messages linking to a GitHub PR with PR-status emoji.",
	"private": true,
	"type": "module",
	"main": "src/index.js",
	"scripts": {
		"ci": "biome check --write && node --test"
	},
	"devEngines": {
		"runtime": {
			"name": "node",
			"version": "24.x"
		}
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.13"
	}
}
